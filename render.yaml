services:
  - type: web
    name: yaradiobot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # 🔥 ИЗМЕНИТЬ НА ЭТО
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
